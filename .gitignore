# Secrets — NEVER commit. Credentials live only in env vars / the host's secret store.
.env
.env.*
!.env.example
*.pem
*service-account*.json
gcp-key*.json
google-ads*.json

# Dependencies & build
node_modules/
.vercel/
.DS_Store
npm-debug.log*

# Scratch
_pwtest.mjs
package-lock.json
